{
  "name": "@3xjn/volt-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "bun run src/index.ts",
    "typecheck": "tsc --noEmit",
    "lint": "biome check .",
    "test": "bun test",
    "smoke": "bun run scripts/smoke.ts",
    "evaluate:search": "bun run scripts/evaluate-search.ts",
    "test:agent": "lune run tests/agent-syntax-contracts.luau",
    "check": "bun run typecheck && bun run lint && bun test && bun run test:agent"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.29.0",
    "zod": "4.3.6"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.11",
    "@types/bun": "1.3.4",
    "typescript": "5.9.3"
  }
}
